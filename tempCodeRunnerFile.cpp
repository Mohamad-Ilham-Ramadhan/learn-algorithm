 n;
   // cin >> n;

   // vector<int>a(n);
   // for (int i = 0; i < n; i++) cin >> a[i];

   // for (int mask = 0; mask < (1 << n); mask++) {
   //    cout << "mask : " << mask << "\n";
   //    int sum = 0;
   //    for (int i = 0; i < n; i++ ) {
   //       if (mask & (1 << i) ) {
   //          sum += a[i];
   //       } else {
   //          sum -= a[i];
   //       }
   //       cout << "sum: " << sum << "\n";
   //    }
   //    if (sum % 360 == 0) {
   //       cout << "YES";
   //       return 0;
   //    }
   // }
   // cout << "NO";